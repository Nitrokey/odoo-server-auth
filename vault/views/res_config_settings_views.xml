<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="base_setup.res_config_settings_view_form" />
        <field name="arch" type="xml">
            <block id="user_default_rights" position="after">
                <block title="Vault" id="vault">
                    <setting
                        string="Vault Share"
                        help="Allow the usage to share secrets with external users"
                        id="vault_share"
                    >
                        <field name="module_vault_share" />
                    </setting>
                    <setting
                        help="Allow all users to import vaults accessible to them"
                        id="vault_import"
                    >
                        <field name="group_vault_import" />
                    </setting>
                    <setting
                        help="Allow all users to export vaults accessible to them"
                        id="vault_export"
                    >
                        <field name="group_vault_export" />
                    </setting>
                    <setting
                        string="Allowed Key Types"
                        help="Restrict which protection methods can be used for newly generated private keys of the vault. Existing keys are not affected."
                        id="vault_allowed_key_types"
                    >
                        <field name="vault_allowed_key_types" />
                    </setting>
                </block>
            </block>
        </field>
    </record>
</odoo>
